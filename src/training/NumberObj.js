import React from 'react'

function NumberObj() {
    let n=1858.69858  // it is number object whatever we store in variable in numberobject it will stored it as number
    // let c=true       //booleaN OBJECT IT only convert string only
    // console.log(Number.EPSILON)
    // console.log(Number.MAX_SAFE_INTEGER)
    // console.log(Number.MAX_VALUE)
    // console.log(n.toExponential())
    console.log(n.toFixed(2))
    console.log(n.toPrecision(2))
    // console.log(n.toString())
    // console.log(c.toString())
  return (
    <div>
      
    </div>
  )
}

export default NumberObj
